version: 2
jobs:
  build:

    working_directory: ~/repo

    machine:
      python:
        version: 2.7
      enabled: true

    steps:
      - checkout
      - run:
          name: Installing
          command: |
            sudo pip install -r requirements.txt